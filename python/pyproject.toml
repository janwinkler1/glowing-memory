[project]
name = "glowing-memory"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "aiohttp",
    "python-dotenv",
    "newspaper3k",
    "langchain",
    "mistralai",
    "lxml-html-clean",
]

[dependency-groups]
dev = [
    "ruff>=0.15.1",
    "ty>=0.0.17",
]
